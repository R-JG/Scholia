export interface UserEntry {
    username: string,
    password: string
};

export interface CreatedUser {
    username: string
};

export interface UserToken {
    username: string,
    token: string
};